%h1{ style: 'float: left;' } All Rounds
= button_to "Add New Round", new_round_path, method: :get, class: 'btn btn-primary', style: 'float: right; margin-top: 25px;'

%table.table.table-responsive.table-hover
  %thead
    %tr
      %th Date Played
      %th Date Added
      %th Course
      %th Total SG
      %th Status
  %tbody
    - @rounds.each do |round|
      %tr{class: "row-link" + (" danger" if round.incomplete?), data: {href: (round_path(round))}}
        %td= round.date_played
        %td= round.created_at.to_date.to_formatted_s
        %td= round.course
        %td= round.total_sg
        %td= round.status
